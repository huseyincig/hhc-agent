# HHC Agent Uninstall

Use the platform uninstall script shipped in the bootstrap archive. Uninstall removes HHC runtime/service integration using the canonical host root. Server-side identity removal remains a separate explicit control-plane operation and must not be silently inferred from local file deletion.
